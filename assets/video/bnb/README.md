NOTE: BnB video assets are omitted from source control, but may be
available separately and can be placed in `assets/video/bnb`.
https://github.com/scottschiller/ArmorAlley/issues/29

`gulp` (default build task) will copy these assets to the `dist` path.
 